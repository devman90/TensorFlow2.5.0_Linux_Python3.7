# Tensorflow 2.5.0 and it's dependencies for linux offline install

*** 'tensorflow-2.5.0-cp37-cp37m-manylinux2010_x86_64.whl' file is not included (>454.3MB) 
*** Download the file from PyPI

# Install command
pip install --no-index --find-links=. -r requirements.txt

(download command: pip download -r requirements.txt)
